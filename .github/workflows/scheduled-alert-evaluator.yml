name: Scheduled Alert Evaluator

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  run-alert-evaluator:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "18"

      - name: Install dependencies
        run: npm install
        working-directory: ./scheduled-alert-evaluator

      - name: Build project
        run: npm run build
        working-directory: ./scheduled-alert-evaluator

      - name: Run alert evaluator
        env:
          MONGODB_URI: ${{ secrets.MONGODB_URI }}
          WEATHER_API_BASE_URL: ${{ secrets.WEATHER_API_BASE_URL }}
        run: node dist/index.js
        working-directory: ./scheduled-alert-evaluator
